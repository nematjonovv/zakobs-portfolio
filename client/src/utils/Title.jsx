
function Title({ title }) {
  return (
    <span className="text-[#8643DC] flex flex-col after:content-[''] after:mb-2.5 after:w-[50px] after:h-0.5 after:bg-[#8643DC]">
      {title}
    </span>
  );
}

export default Title;
